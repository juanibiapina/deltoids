@@ -1,8 +1,8 @@ [class_declaration Outer]
 class Outer {
-  limit = 1;
+  limit = 5;
 
   run() {
-    const total = this.limit * 2;
+    const total = this.limit * 3;
     console.log(total);
   }
 }
