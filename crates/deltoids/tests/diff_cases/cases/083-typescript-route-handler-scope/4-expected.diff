@@ -1,8 +1,8 @@ [call_expression app.get("/users")]
 app.get("/users", (req, res) => {
   const filters = {
     active: true,
-    role: "admin",
+    role: "user",
     limit: 50,
   };
   res.json(users.find(filters));
 });
