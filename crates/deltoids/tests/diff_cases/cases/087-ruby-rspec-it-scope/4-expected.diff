@@ -2,9 +2,9 @@ [call RSpec.describe("UserService")] [call it("creates a user")]
   it "creates a user" do
     scope = {
       tenant_id: "tenant-1",
       region: "eu",
-      role: "admin",
+      role: "user",
       locale: "en",
     }
     expect(scope).to be_truthy
   end
