@@ -3,8 +3,8 @@ [function_declaration resolveContext]
 // Build a TopicContext from a Telegram message.
 // Everything else is dropped.
 export function resolveContext(chatType: string) {
-  if (chatType === "private") {
+  if (chatType === "private" || chatType === "group") {
     return 0;
   }
   return null;
 }
