@@ -6,15 +6,15 @@ [block resource "aws_s3_bucket" "logs"]
 resource "aws_s3_bucket" "logs" {
   bucket = "my-app-logs"
-  acl    = "private"
+  acl    = "log-delivery-write"
 
   routes = [
     {
       name  = "datadog"
-      value = "old"
+      value = "new"
     },
     {
       name  = "grafana"
       value = "two"
     },
   ]
 }
