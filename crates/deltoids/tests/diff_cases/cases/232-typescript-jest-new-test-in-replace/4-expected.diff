@@ -2,4 +2,5 @@ [call_expression describe("client")] [call_expression it("falls back to a default")]
-  it("falls back to a default", () => {
+  it("recovers via the slow path", () => {
     setupStub();
-    expect(result).toEqual("default");
+    expect(result).toEqual({ githubId: 1, login: "ghost" });
+  });
   });

@@ -5,0 +7,4 @@ [call_expression describe("client")] [call_expression it("raises when recovery returns nothing")]
+  it("raises when recovery returns nothing", () => {
+    setupStub();
+    expect(() => run()).toThrow(RecoveryError);
+  });
