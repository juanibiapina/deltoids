mod tests {
    #[test]
    fn one() {
        assert_eq!(work(1), 2);
    }

    #[test]
    fn two() {
        assert_eq!(work(2), 2);
    }

    #[test]
    fn three() {
        assert_eq!(work(3), 3);
    }
}
