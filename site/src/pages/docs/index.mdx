---
layout: "../../layouts/DocsLayout.astro"
title: "Install"
description: "Install deltoids: one package, every binary."
---

import { SITE } from "../../data/site";

# Install

```bash
brew install juanibiapina/taps/deltoids
```

From source:

```bash
cargo install --git https://github.com/juanibiapina/deltoids deltoids-cli
```

A shell installer is also published on each
<a href={`${SITE.repo.url}/releases`}>GitHub release</a>.

One package ships every binary. Run `deltoids` in a repo to open the TUI;
pipe a diff to use it as a pager.

## Next

Wire `deltoids` into the tool you already review code in:
[Git](/docs/integrations/git/), [GitHub CLI](/docs/integrations/gh/), or
[Lazygit](/docs/integrations/lazygit/).

To trace what a coding agent changes, install the agent tools for
[pi](/docs/integrations/pi/) or
[Claude Code](/docs/integrations/claude-code/).
